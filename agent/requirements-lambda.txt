# Runtime deps for the Lambda bundle.
# Kept separate from pyproject.toml because the Lambda build pins the target
# platform (manylinux x86_64) so compiled wheels match the function architecture
# regardless of the build host (Apple Silicon vs CI x86).
# boto3/botocore are provided by the Lambda runtime — excluded to keep the zip small.
httpx>=0.27
pydantic>=2.9
beautifulsoup4>=4.12
trafilatura>=1.12
pyyaml>=6.0
google-genai>=1.0
